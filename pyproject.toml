[project]
name = "milo"
version = "0.1.0"
description = "milo — offline-first agentic coding CLI for local LLMs (LM Studio / Ollama / vLLM)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "openai>=1.30.0",
    "rich>=13.7.0",
]
